//mod WavReader;
//use crate::WavReader::WavReader;

//TODO : importer WAvReader et tester
fn main (){
   /* 
   let file_path = "";
    match WavReader::new(file_path) {
        Ok(reader) => {
            let spectrum = reader.fft();
            println!("Spectre de fréquences: {:?}", spectrum);
        }
        Err(e) => println!("Erreur: {}", e),
    }
    */
}